FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o ra ./cmd/ra

FROM alpine:3.19
RUN apk add --no-cache ca-certificates wget
COPY --from=builder /app/ra /ra
EXPOSE 8443
CMD ["/ra"]
